/* ── config.js ──
   API base URL and constants.
   开发环境：http://localhost:8000
   生产环境：留空 ''（如果前后端同域部署）或改成实际地址 */

window.Pages = {};
const API_BASE = '';
const TOKEN_KEY = 'hermes_panel_token';
const THEME_KEY = 'hermes_panel_theme';
